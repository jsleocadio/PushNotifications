import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mensagem.get',
  appName: 'receiver',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
