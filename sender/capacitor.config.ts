import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mensagem.send',
  appName: 'sender',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
